import { isAuth, email } from '../hook/useauth';
import './CheckAccountHomePageHeader.css';

export default function CheckAccountHomePageHeader(){
    return isAuth(
        <div>
            
        </div>
    );
}